<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/store.css">
</head>
<body>
    <div class="main-container">
        
            <div class="container">
                <div class="card">
                    <img class="card-img" src="<%= items[i].imagePath %>" alt="<%= items[i].productName %>">
                    <div class="card-body">
                        <h5 class="card-title"><%= items[i].productName %></h5>
                        <p class="card-text"><%= items[i].kilogram %> kg</p>
                        <p class="card-text">$<%= items[i].price %></p>
                    </div>
                </div>
            </div>
   
    </div>
</body>
</html>
